# Codex Feedback for Claude

Owner: Codex. Claude should read this file every 10-15 minutes and before starting a new task chunk.

## Current Review State

Started: 2026-06-19T01:03:45+09:00
Baseline HEAD: 3059f0a

Pre-existing dirty worktree at monitor start:

- `M docs/ROADMAP.md`
- `?? docs/positioning-vs-mcp.md`

These baseline changes are treated as pre-existing work, not Codex findings.

Active blocking feedback: none.

## Rules for Claude

- Treat new entries under "Codex Review Notes" as high-priority review feedback.
- Address concrete Codex findings before expanding scope or starting unrelated work.
- Keep changes scoped to the current task and repo conventions.
- Do not overwrite unrelated user/Codex changes.
- Do not edit this file except to add a short response under a note that explicitly asks for one.
- Update `CLAUDE_STATUS.md` with current task, changed files, validation run, and blockers.

## Codex Review Notes

- 2026-06-19T01:03:45+09:00: Monitor started. No blocking feedback yet.
- 2026-06-19T01:06:00+09:00: Blocking review note for the federation channel change in `packages/core/src/schema.ts` and `packages/core/src/store.ts`.
  `DocStore.snapshot()` and `DocStore.snapshotOf()` now emit `federation`, but `DocStore.fromSnapshot()` and `DocStore.importSnapshot()` do not restore/import `snap.federation`. This will silently lose federation sources during backup restore, AI snapshot import, or server-side snapshot reconstruction. Please implement the missing parse/write path, decide whether absent `snap.federation` preserves the live channel or defaults to empty using the existing comments/views semantics, and add focused tests for roundtrip behavior.
- 2026-06-19T01:08:05+09:00: The federation snapshot restore/import issue appears addressed in `packages/core/src/store.ts`, with focused coverage in `packages/core/test/federation.test.ts`. Validation passed: `corepack pnpm --filter @figcad/core test` -> 29 test files passed, 327 tests passed. No active blocking feedback.
- 2026-06-19T01:10:10+09:00: Claude committed the reviewed A1+A2 work as `24bb355` (`feat(M13-A): federation channel`). Commit scope matches the reviewed federation schema/store/test changes. No active blocking feedback.
- 2026-06-19T11:24:54+09:00: Codex monitoring was stopped at the user's request. The Windows scheduled task `CodexFigcadMonitor` has been deleted. No further automatic watchdog updates should be expected unless the user restarts monitoring.
